# places-agent — admin UI design

Operator management web on **`places.agent-mate.ai`**. Pixel and interaction contract for Features 14–19. Stories: [`1.agent-stories.md`](./1.agent-stories.md). ACs: [`2.agent-ac.md`](./2.agent-ac.md). Clickable mock-ups: [`ui-mockup/`](./ui-mockup/). App Router map: [`5.agent-design.md`](./5.agent-design.md).

This document covers **app** chrome only. Consumer what2eat / where2play screens are out of scope.

---

## 1. Subject

| | |
| --- | --- |
| **Product** | Operator app for the place gateway identified as `places-agent` |
| **Audience** | Ethan and invited admins |
| **Job of the UI** | Sign in, invite admins, issue and revoke **caller API keys**, read how to call HTTP and MCP |

Callers never use this UI. Map-vendor keys (AMAP / Google / Tripadvisor) never appear here.

---

## 2. Direction

**性冷淡**, same family as [kb.agent-mate.ai](https://kb.agent-mate.ai): off-white field, black ink, hairline rules, zero radius, leftover space.

Personality sits in **one** place: `agent-logo.png` (hand-drawn blobs on `--bg` `#fafafa`) as the mark and the browser tab icon. Everything else stays quiet.

| Do | Do not |
| --- | --- |
| Hairline dividers, uppercase mono labels, black rectangle buttons | Shadows, gradients as decoration, border-radius, color status pills |
| Weight and underline for state (active nav, active locale) | Icons in the left nav |
| Show a caller secret once (create / regenerate) | “View secret again” from the list |
| Four locale codes `EN CN HK TW` | A two-way 中文 / EN switcher (HK and TW would collapse) |

Motion: one short rise on public/auth first paint (`12px`, `700ms`). Respect `prefers-reduced-motion`. No ambient animation on signed-in tables.

---

## 3. Tokens

```css
--bg: #fafafa;
--bg-elevated: #ffffff;
--ink: #0a0a0a;
--ink-2: #1f1f1f;
--mute: #525252;
--mute-soft: #6b6b6b;
--line: #e0e0e0;
--line-strong: #bdbdbd;
--fill: #f0f0f0;
--danger: #8b1a1a;
--radius: 0;
--control-h: 2.75rem;
--font-ui: "Outfit", "Noto Sans SC", "Noto Sans TC", system-ui, sans-serif;
--font-cn: "Noto Sans SC", "Noto Sans TC", "Outfit", system-ui, sans-serif;
--font-mono: "JetBrains Mono", ui-monospace, monospace;
--max: 760px;
```

Load **Noto Sans TC** in addition to SC so `HK` / `TW` glyphs stay Traditional. `CN` uses SC.

Public/auth canvas may use a faint vertical wash (`#f7f7f7` → `#fafafa` → `#f0f0f0`) matching kb. No colored wash.

**Type scale**

| Role | Face | Size | Weight | Notes |
| --- | --- | --- | --- | --- |
| Wordmark | Outfit | 1.35rem home / 1.2rem header | 500 | Hostname, lowercase, not translated |
| Page title | Outfit | 1.5–1.85rem | 600 | |
| Body | Noto Sans SC/TC | 1.05rem / 1.65 line | 400 | |
| Eyebrow / table header / field label | JetBrains Mono | 0.75rem | 500 | Uppercase, tracking ~0.12em |
| Button | Outfit | 0.8125rem | 500 | Tracking 0.08em; height 2.125rem on public/auth, 2.75rem in app headers |
| Secret / prefix / code | JetBrains Mono | 0.9–0.95rem | 400 | |

**Logo sizes:** home/auth `56×56`; header `36×36`; favicon `32×32` PNG + `180×180` apple-touch. Source file: `agent-logo.png` (600×600). **Favicon/apple-touch:** flood-fill `#fafafa` field to **transparent** so the tab shows only the mark (no white tile on the browser chrome). Page `<img>` keeps the full PNG on `--bg`. `alt` empty when the wordmark sits beside it; `aria-label` is the hostname.

---

## 4. Shells

Four shells. Locale switcher is on every shell (Feature 19).

```text
PUBLIC HOME                         AUTH (login / reset / set password)
┌─────────────────────────────┐     ┌─────────────────────────────┐
│                    EN CN HK TW│     │                    EN CN HK TW│
│                               │     │  [logo] places.agent-mate.ai │
│  [logo] places.agent-mate.ai  │     │  notice (register closed)    │
│  tagline                      │     │  title                       │
│  ─────────────────────────    │     │  fields                      │
│  instructions link            │     │  [Sign in]  Reset password   │
│  [Sign in]                    │     │  Back to home                │
│                    copyright  │     │                    copyright  │
└─────────────────────────────┘     └─────────────────────────────┘

GUIDE (public)                      APP (signed-in)
┌─────────────────────────────┐     ┌─────────────────────────────┐
│ [logo] host   Back  EN CN…  │     │ [logo] host  Hello, {name}   │
│ CONNECT                     │     │ instructions   EN CN HK TW   │
│ Agent instructions          │     ├────────┬────────────────────┤
│ toc · body · diagrams       │     │ Keys   │ EYEBROW            │
│                    copyright│     │ Admins │ Title              │
└─────────────────────────────┘     │ Sign out│ table / form      │
                                    └────────┴────────────────────┘
```

| Shell | Routes (conceptual) | Chrome |
| --- | --- | --- |
| Public home | `/` | Centered column `max-width: 26rem`, left-aligned inside. No sidebar. |
| Auth | `/login`, `/reset-password`, `/set-password` | Same column. Register notice on login only. |
| Guide | `/instructions` | Thin header, content `max-width: 56rem` (`--guide-max`). Same URL signed-in or not; signed-in visitors keep the **app** header + left nav and drop the public “Back to home”. |
| App | `/admin/api-keys`, `/admin/api-keys/new`, `/admin/api-keys/:id`, `/admin/users` (`/admin` → keys) | Header + `200px` text nav. Default landing after login = **Keys**. |

Unsigned visitors who open an app route go to login (AC: landing shell is not shown).

**Left nav items** (i18n keys, no icons):

1. `admin.nav.keys` → keys list (landing)
2. `admin.nav.admins` → admin users
3. `admin.nav.sign_out` → ends session, returns to home

Active item: ink color, weight 500, `2px` inset bar on the left. Sign out stays mute; it is never “active”.

**Header (app):** mark + hostname (home) · `admin.landing.instructions_link` (new tab, `rel="noopener noreferrer"`, same as the home instructions link) · `admin.landing.hello` with `{name}` · locale switcher.

**Locale switcher:** four **protocol ids** `EN` `CN` `HK` `TW` (not translated). Active = ink + underline. Persist in the browser for the mock-up; production may use cookie or `Accept-Language` override. `html lang`: `en` / `zh-CN` / `zh-HK` / `zh-TW`.

**Footer:** `admin.footer.copyright` right-aligned. Optional skip control `admin.a11y.skip` top of DOM (visually hidden until focus). On narrow viewports, left nav collapses behind `admin.nav.menu`.

---

## 5. Components

All user-facing strings are **keys**. Selectors for tests: `data-i18n` and `data-testid`. Do not assert English literals.

| Component | Spec |
| --- | --- |
| **Primary button** | Black fill, white type, 1.5px ink border, radius 0. Label is a verb: Sign in, Issue new key, Save, Invite. |
| **Text link** | Ink, 1px `line-strong` underline; hover underline → ink. Used for instructions, reset, back, row actions. |
| **Text button (danger-quiet)** | Mute type, no fill. Hover → ink. Delete / Revoke / Regenerate sit here, not as red pills. |
| **Field** | Mono uppercase label above; value is a bottom-border input (no box). Fill is canvas `--bg` (`#fafafa`), same as the public/auth/app page — including Chrome/Safari autofill (no yellow/blue browser tint). Boxed invite field stays `--fill`. Focus: bottom border + 2px ink outline offset. Password: eye toggle, `aria-pressed`. |
| **Notice** | `fill` background, 1px `line` border, mute body. Login closed-register plate: prefix + underlined `admin.register.contact_admin` + suffix + protocol `api-key` in `.auth-status-key`. Hover/focus on the contact control shows the WeChat QR popover (same interaction as kb.agent-mate.ai). Errors: `danger` type, key such as `errors.login_failed`. |
| **Table** | No vertical rules. Header row mono uppercase. Keys list: leading square checkbox column (select all in the header; row boxes with `admin.keys.select_row`). Row actions are text links in a trailing cell: Edit · Regenerate · Delete. Prefix column is mono. Status `ACTIVE` is ink; other statuses mute. |
| **Secret panel** | Mono secret, copy control `admin.keys.copy`. Warning that the secret will not be shown again. |
| **Dialog** | Sharp rectangle, 1px line, no shadow beyond a 28% ink scrim. Confirm regenerate / delete / bulk delete. Escape closes. |
| **Code block** | 1.5px `line-strong` border, `#f4f4f4` fill, mono, copy control. Machine id `places-agent` and hostnames stay literal. |
| **Flow diagram** | Hairline boxes. The **Caller API key** node is the only inverted block (black fill, off-white type). |

Keyboard: visible `2px` ink focus rings. Escape closes dialogs. Primary actions are reachable without a pointer.

---

## 6. Screens

Mock-up files live under [`ui-mockup/`](./ui-mockup/). English in the table is **reference copy**, not the contract.

### 6.1 Public home — `places-agent-admin-home`

**File:** `01-home.html`

| Element | Key | Notes |
| --- | --- | --- |
| Mark + hostname | — | Hostname is not localized |
| Tagline | `admin.home.tagline` | |
| Instructions | `admin.home.instructions_link` | → guide, new tab |
| Sign in | `admin.home.login` | Primary button → login |
| Locale | — | `EN CN HK TW` |
| Footer | `admin.footer.copyright` | |

### 6.2 Login — `places-agent-admin-users` US1, US6

**File:** `02-login.html` (`?error=1` = failed)

| Element | Key | Notes |
| --- | --- | --- |
| Closed-register plate | `admin.register.disabled_prefix` | EN reference: `Open registration is closed. ` (trailing space) |
| Contact trigger | `admin.register.contact_admin` | Underlined text button. `data-testid="contact-admin"` |
| Suffix | `admin.register.disabled_suffix` | EN reference: ` for an ` |
| Protocol id | — | Literal `api-key` in `<code class="auth-status-key">` (mono, not translated) |
| WeChat QR image | `admin.register.wechat_qr_alt` | Same asset as kb: `EthanWeChat.png` |
| QR caption | `admin.register.wechat_qr_caption` | Mono, uppercase via CSS (`SCAN WITH WECHAT` in EN) |
| Title | `admin.login.title` | |
| Username or email | `admin.login.username` | |
| Password | `admin.login.password` | |
| Submit | `admin.login.submit` | → keys landing |
| Reset | `admin.login.reset_link` | |
| Back | `admin.common.back_home` | |
| Wrong password | `errors.login_failed` | |

The plate stays `.auth-status` (`fill`, hairline, radius 0). Do not put `data-i18n` on the `<p>` — locale apply would wipe the nested trigger and popover. Keep `data-testid="register-disabled"` on the plate.

**Contact-admin popover (match kb.agent-mate.ai):**

- Trigger is a `button.contact-admin-trigger` (not a link). Ink underline (`line-strong` → `ink` on hover/focus). Keyboard: `:focus-visible` ink ring; `:focus-within` opens the same popover as hover.
- Popover `.contact-admin-pop` sits **above** the trigger (`bottom: calc(100% + 0.55rem)`, centered). White elevated fill, 1px `line`, `--radius: 0`. QR `180×180`. Caption under the image.
- Auth column sits lower than home so the popover is not clipped by the viewport: `.auth-main` top padding `clamp(8rem, 14vh + 4.5rem, 12.5rem)`. Home stays `max(4.5rem, min(14vh, 7.5rem))`.
- Hidden until `.contact-admin:hover` or `:focus-within` (opacity/visibility). Include a hover bridge (`::after` below the pop) so the pointer can reach the QR.
- Motion: 0.16s opacity + 4px rise. `prefers-reduced-motion: reduce` → no transition.
- Meaning is not color-only: underline + tooltip + caption.

```text
┌─────────────────────────────────────┐
│     [logo] places.agent-mate.ai     │
│              ↕                      │
│     ┌─────────────────────────┐     │
│     │  [ WeChat QR ]          │     │  ← hover / focus
│     │  SCAN WITH WECHAT       │     │
│     └─────────────────────────┘     │
│     ┌─────────────────────────┐     │
│     │ Open registration is    │     │
│     │ closed. Contact an      │     │  ← Contact an admin underlined
│     │ admin for an api-key    │     │
│     └─────────────────────────┘     │
│     Admin sign-in                   │
│     form · Sign in · Reset          │
└─────────────────────────────────────┘
```

Username and password fields use canvas `--bg` for their fill (including autofill). They must not read as a separate blue or yellow slab.

No register control. Username `admin` and email `me@ethanhuang.com` are data, not copy.

### 6.3 Reset password — US3

**File:** `03-reset.html` (`?sent=1`)

Email field + `admin.reset.submit`. Success: single `callout-success` with `admin.reset.sent` (includes junk-folder note); form and lead hidden. Failure: keyed error, password unchanged.

**Email layout (Resend):** `12-email-reset.html` — table HTML, 性冷淡 card on `#fafafa`. Must include: logo + hostname, eyebrow, heading, who/why copy, black CTA button, plain-link fallback, ignore-if-not-you line, footer (`admin.mail.*` keys). Invite reuses the same shell: `13-email-invite.html`. Invite CTA links to **`/accept-invite?token=…`**, not set-password. Production sends **HTML + text** (text mirrors link).

### 6.4 Accept invite — US4

**Route:** `/accept-invite?token=…` (implemented; dedicated mock-up file TBD)

| Element | Key |
| --- | --- |
| Title | `admin.accept_invite.title` |
| Lead | `admin.accept_invite.lead` |
| Invited email | `admin.accept_invite.email_lead` + address |
| First name | `admin.accept_invite.first_name` |
| Last name | `admin.accept_invite.last_name` |
| Username | `admin.accept_invite.username` |
| Password / confirm | `admin.accept_invite.password`, `admin.accept_invite.confirm` |
| Submit | `admin.accept_invite.submit` |
| Success | `admin.accept_invite.done_*` callout + `admin.accept_invite.sign_in` → login |
| Expired token | `errors.invite_link_expired_*` callout only |
| Pending invite in admin list | `admin.users.name_pending` (` -- `) until onboarding completes |

Form must submit via **POST** (server action or API). Credentials must not appear in the URL after submit.

### 6.5 Set password — US5 (reset and empty session only)

**File:** `04-set-password.html`

| Variant | Query | Purpose |
| --- | --- | --- |
| Empty hash (signed-in) | (default) | `admin.set_password.lead` — first boot / empty password |
| Reset link | `?token=…` | `admin.set_password.reset_lead` only |
| Saved | `?done=1` | Success callout + **`admin.set_password.sign_in`** button → login |
| Expired token | expired callout | Expired **callout only** (logo + callout + action); no form |
| Mismatch | inline error | `errors.password_mismatch` inline above fields |

Invite onboarding is **not** on this screen — use §6.4 accept-invite.

Callouts use `.callout` / `.callout-error` / `.callout-success` (eyebrow + title + body + optional action). Do not stack conflicting leads with a one-line error.

### 6.6 Agent instructions — `places-agent-admin-instructions`

**Files:** `05-instructions.html` (public header), `11-instructions-app.html` (app shell)

Same body. Must state, in keys:

1. How to call HTTP
2. How to call MCP
3. How to send a **caller API key** (`Authorization: Bearer`)
4. Agent id is the literal **`places-agent`** (never replaced by a translated catalog value)
5. Agent capabilities (tools on HTTP + MCP; Place chat and `Tripadvisor.enrich` HTTP only)

Suggested section order (this *is* a sequence): Agent capabilities → Architecture → Get a key → HTTP → MCP.

**Agent capabilities:** First section. TOC + heading `admin.guide.h_capabilities`. Table (`data-testid="guide-capabilities-table"`): Capabilities · Channel · Description. First six Capabilities cells are tool-name literals in `<code>`: `search_restaurants`, `search_places`, `get_place_details`, `geocode`, `navigate`, `plan_itinerary`. Last two: Place chat (`admin.guide.cap_chat`) and literal `Tripadvisor.enrich`. Channel is `admin.guide.cap_channel_both` (`HTTP and MCP`) or `admin.guide.cap_channel_http` (`HTTP only`). Place chat channel also shows `POST /v1/chat`. Section selector: `data-testid="guide-capabilities"`. Architecture does not duplicate the tool list.

Diagram: wide layout reads left to right — HTTP and MCP lanes join at **Caller API key**, then `places-agent`, then map vendors (mute; not configurable here). Narrow viewports stack the same sequence.

Entries: home link `admin.home.instructions_link`; signed-in header `admin.landing.instructions_link`. Both open `/instructions` in a new tab.

### 6.7 Keys list (landing) — `places-agent-admin-landing` + `places-agent-admin-api-keys`

**File:** `06-keys.html` (`?empty=1`)

Default screen after login. Eyebrow `admin.keys.eyebrow` (**API KEYS**) on its own row. Title `admin.keys.title` is not shown. Lead `admin.keys.lead` sits on the next row with Issue and Bulk Delete (`min-height` matches the compact control). No hairline under the page head. List sits **2.5rem** below that row. Keys-table cells share one `1rem` size.

`admin.keys.issue` and `admin.keys.bulk_delete` share `.page-head-actions` on the lead row: Issue immediately left of Bulk Delete, compact `btn btn-page` so the row fits. Bulk Delete is enabled (ink fill, white type) when at least one row is checked; disabled (paper fill `--fill`, mute type, `--line-strong` hairline) when none are. Do not hide Bulk Delete. Empty state uses `admin.keys.empty` only — Issue stays in the page head (`data-testid="issue-key"`).

| Column | Content |
| --- | --- |
| (checkbox) | Header: select all (`admin.keys.select_all`). Row: `admin.keys.select_row` with `{name}`. Square, radius 0. |
| `admin.keys.col_name` | Admin-given name |
| `admin.keys.col_prefix` | Public prefix only (mono). Never the full secret. |
| `admin.keys.col_status` | `admin.keys.status_active` |
| `admin.keys.col_issued` | Locale-aware date |

Row actions: `admin.keys.edit` · `admin.keys.regenerate` · `admin.keys.delete`.

Bulk delete confirm: `admin.keys.delete_selected_title` / `admin.keys.delete_selected_body` (`{count}`) / `admin.keys.delete_selected_submit`.

Empty: `admin.keys.empty` + control to create. No checkboxes when the table is hidden.

### 6.8 Create key — API keys US1

**Files:** `07-key-new.html`, `08-key-created.html`

New: name, description, submit generates a secret.

Created: plaintext secret **once**, copy via `admin.keys.copy`, warning `admin.keys.created_warn`. No later “view full secret” on the list (stricter than kb’s Users “View”).

### 6.9 Edit key — US2–US4

**File:** `09-key-edit.html` (`?confirm=regenerate` \| `delete`)

Edit name/description without rotating. Regenerating shows a new secret (same created-secret pattern) and the previous secret stops working. Delete removes the row. Confirm in a sharp dialog.

### 6.10 Admins — users US4

**File:** `10-admins.html`

Eyebrow `admin.users.eyebrow` on its own row (no duplicate `admin.users.title` heading). Lead `admin.users.lead` uses the same `.page-head-row` as Keys so the gap under the subject matches. Invite is the next row: boxed email (`input-box`, fill `--fill`) with `admin.users.invite_submit` immediately to its right. No hairline under the page head. List sits **2.5rem** below the invite row. Columns: name, email, status (`ACTIVE` / `PENDING`). Invited admins must set a password before the landing works.

---

## 7. i18n

Catalogs: `EN` (default), `CN`, `HK`, `TW`. Missing key → `EN` → the key string. Page stays up (Feature 19 US3).

`HK` and `TW` are both Traditional and **must differ in wording** (not OpenCC of `CN`). Protocol ids not translated: `places-agent`, `EN`/`CN`/`HK`/`TW`, `AMAP`, `GOOGLE_MAPS`, `TRIPADVISOR`, `OPEN_METEO`, default username `admin`, default email `me@ethanhuang.com`. Weather **labels** are not protocol ids — they use `weather.wmo.{code}` catalogs (ADR-014).

Locale-sensitive dates on the keys table (e.g. `2026-08-17` may stay ISO in `EN`; `CN`/`HK`/`TW` may use `YYYY年M月D日`).

Mock-ups apply keys through `data-i18n` and `assets/i18n.js` (catalogs inlined so `file://` works).

---

## 8. Copy rules

- Name controls by what the operator does: Issue new key, Copy secret, Regenerate, Delete, Invite admin.
- Empty keys: invite to issue the first key.
- Errors name the failure and the next step (`errors.login_failed`, `errors.password_required`). No apology theater.
- Do not mention map-vendor keys, Portainer, or Quanzil in this UI.

---

## 9. Quality bar (UI)

- Desktop (~1280) and mobile (~390): public/auth column remains readable; app nav becomes a text menu.
- Focus order: skip → locale → main landmarks → primary action.
- Escape closes dialogs.
- Status is not color-only (`ACTIVE` is a word).
- Secrets never go to `localStorage` in production. Mock-up may stash locale only.
- Critical journeys for later E2E: home → login → keys; create key + copy; locale switch EN→HK; closed register notice; contact-admin WeChat QR on hover/focus.

---

## 10. Mock-up index

| File | Screen |
| --- | --- |
| `ui-mockup/index.html` | Screen list |
| `01-home.html` | Public home |
| `02-login.html` | Login; `?error=1` failed |
| `03-reset.html` | Reset request; `?sent=1` |
| `04-set-password.html` | Reset / empty-password set; `?done=1`, expired callout |
| `14-accept-invite.html` | Accept invite onboarding; `?done=1` success |
| `12-email-reset.html` | Reset mail (Resend HTML) |
| `13-email-invite.html` | Invite mail (Resend HTML); CTA → `/accept-invite?token=…` |
| `05-instructions.html` | Guide, public |
| `06-keys.html` | Landing / key list; `?empty=1` |
| `07-key-new.html` | Create key |
| `08-key-created.html` | One-time secret |
| `09-key-edit.html` | Edit / confirm regenerate or delete |
| `10-admins.html` | Admins + invite |
| `11-instructions-app.html` | Same guide, signed-in shell |

Open `ui-mockup/index.html` in a browser (or any local static server). Locale switcher is live on every screen.
