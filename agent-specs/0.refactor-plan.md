# Places-Agent 产品重构计划

## Context

places-agent MVP-1/2 快速交付后暴露质量问题：搜索不准（上海搜到美国餐厅）、无图片、行程牵强、中英文 prompt 混杂、服务器不稳定、Admin 后台 bug。现按用户影响分级，拆为 9 个细粒度批次，每批 1–3 天可独立交付。

---

## 方法论：Spec → Test → Implement

每批严格遵循：

1. **更新 Specs** — [`agent-stories.md`](./agent-stories.md)
2. **更新设计** — [`agent-design.md`](./agent-design.md)
3. **更新测试** — [`agent-test-plan.md`](./agent-test-plan.md) 追加 TC-M*-* 测试矩阵
4. **TDD Red** → **Green** → **Refactor**
5. **验收** — `make quality` 全绿

---

## 总览

| 批次 | 代号 | 核心目标 | 预估 | 新文件 | 改文件 | 测试数 |
|------|------|---------|------|--------|--------|--------|
| 1 | **MVP-3a** ✅ | 服务器稳定 + Provider 自动选择 | ~~2–3 天~~ 完成 | 4 | 3 | 17 |
| 2 | **MVP-3b** | Photos + Price Level | 2–3 天 | 4 | 5 | 16 |
| 3 | **MVP-4a** | 语言路由 + 搜索关键词映射 | 2–3 天 | 4 | 3 | 12 |
| 4 | **MVP-4b** | 行程泛化（去硬编码 + 餐食上下文） | 2–3 天 | 2 | 2 | 10 |
| 5 | **MVP-5a** | Admin API 加固 + Error Boundary | 1–2 天 | 2 | 6 | 8 |
| 6 | **MVP-5b** | 测试补全（覆盖率提升） | 1–2 天 | 3 | 0 | 12 |
| 7 | **MVP-6a** | TripAdvisor enrich → MCP tool | 1–2 天 | 2 | 1 | 5 |
| 8 | **MVP-6b** | E2E 补全（密码重置 + caller 集成） | 1–2 天 | 3 | 0 | 8 |
| 9 | **MVP-7** | Prompt 组装器 + 多语言 prompt + 收尾 | 2–3 天 | 3 | 2 | 8 |

**依赖关系：**

```
MVP-3a ──→ MVP-3b ──→ MVP-4a ──→ MVP-4b
                         │
MVP-5a（独立）            ├──→ MVP-7
MVP-5b（独立）            │
MVP-6a ◄── MVP-3b        │
MVP-6b（独立）            │
```

---

## 批次 1: MVP-3a — 服务器稳定 + Provider 自动选择 ✅ 完成 (2026-08-20)

> 先止血（不再崩溃），再解决搜索不准。不涉及 photos/price。

### Specs 更新

| 文件 | 变更 |
|------|------|
| `agent-stories.md` | Feature 22 (provider-auto) + Feature 23 (server-stability) + AC |
| `agent-test-plan.md` | §18 追加 TC-M3a-* |

### 实现

**Story A: 服务器稳定** (Feature 23)

| 操作 | 文件 | 变更 |
|------|------|------|
| 修改 | `server.ts` | `readJsonBody()` try-catch；SIGTERM/SIGINT graceful shutdown |
| 新增 | `src/mcp/session-manager.ts` | SessionManager (TTL 30min + 定期清理) |
| 新增 | `src/mcp/session-manager.test.ts` | 4 个单元测试 |

**Story B: Provider 自动选择** (Feature 22)

| 操作 | 文件 | 变更 |
|------|------|------|
| 新增 | `src/adapters/china-cities.ts` | ~350 个中国城市 + 英文映射 |
| 新增 | `src/adapters/provider-resolver.ts` | `resolveProviderStrategy()` — 策略1/策略2 矩阵 |
| 新增 | `src/adapters/provider-resolver.test.ts` | 9 个单元测试 |
| 修改 | `src/core/tools.ts` | search 入口集成 resolver |
| 修改 | `server.ts` | 替换裸 Map 为 SessionManager |

### 测试矩阵 (17 个)

| ID | ★ | 类型 | 主题 |
|----|---|------|------|
| TC-M3a-S01 | ✓ | Unit | readJsonBody malformed → {ok:false} |
| TC-M3a-S02 | ✓ | Unit | SessionManager TTL 清理 |
| TC-M3a-S03 | | Unit | SessionManager close() 清空 |
| TC-M3a-S04 | | Unit | SessionManager 未过期保留 |
| TC-M3a-PS01 | ✓ | Unit | 上海 + CN → AMAP only |
| TC-M3a-PS02 | ✓ | Unit | 上海 + EN → Google + AMAP |
| TC-M3a-PS03 | ✓ | Unit | 香港坐标 + HK → Google + AMAP |
| TC-M3a-PS04 | ✓ | Unit | 台湾 + TW → Google only |
| TC-M3a-PS05 | ✓ | Unit | 东京 + EN → Google only |
| TC-M3a-PS06 | ✓ | Unit | 显式 providers 覆盖 |
| TC-M3a-PS07 | | Unit | 中国城市列表文本匹配 |
| TC-M3a-PS08 | | Unit | 坐标范围判断 |
| TC-M3a-PS09 | | Unit | enrichProviders 含 TRIPADVISOR |
| TC-M3a-H01 | ✓ | HTTP | 中国地址无 providers → 中国结果 |
| TC-M3a-H02 | ✓ | HTTP | 东京无 providers → 日本结果 |
| TC-M3a-H03 | ✓ | HTTP | 发送非 JSON → 400 |
| TC-M3a-H04 | | HTTP | 显式 providers 覆盖 |

### 验收

- [x] TC-M3a-* 全部 17 个测试通过 (295/295)
- [x] `make quality` 全绿 (tsc 0 errors, lint 0 errors on new files)
- [x] coverage: Statements 90.56%, Lines 94.74%
- [x] Live smoke: 大陆→AMAP only, 香港→Google+AMAP, 海外→Google only, 非JSON→400

---

## 批次 2: MVP-3b — Photos + Price Level

> 丰富搜索结果字段。依赖 MVP-3a。

### Specs 更新

| 文件 | 变更 |
|------|------|
| `agent-stories.md` | Feature 20 (photos) + Feature 21 (price-level) + AC |
| `agent-test-plan.md` | §18 追加 TC-M3b-* |

### 实现

**Story A: Price Level** (Feature 21)

| 操作 | 文件 | 变更 |
|------|------|------|
| 修改 | `src/core/types.ts` | PlaceCard 增加 `price_level?`, `price_per_person?` |
| 新增 | `src/core/price.ts` | 归一化函数 (Google PRICE_LEVEL_* / AMAP cost → $/$$/$$$) |
| 新增 | `src/core/price.test.ts` | 4 个测试 |
| 修改 | `src/adapters/google/direct.ts` | fieldMask 加 `places.priceLevel` |
| 修改 | `src/adapters/google/card-mapper.ts` | DirectPlace 加 priceLevel → 映射 |
| 修改 | `src/adapters/amap/direct.ts` | show_fields 加 cost |
| 修改 | `src/adapters/amap/card-mapper.ts` | AmapBusiness 加 cost → 映射 |

**Story B: Photos** (Feature 20)

| 操作 | 文件 | 变更 |
|------|------|------|
| 新增 | `src/adapters/google/photos.ts` | Google photo URL 拼接（feature flag `GOOGLE_PHOTOS_ENABLED`） |
| 新增 | `src/adapters/google/photos.test.ts` | 3 个测试 |
| 修改 | `src/adapters/google/direct.ts` | fieldMask 加 `places.photos` |
| 修改 | `src/adapters/google/card-mapper.ts` | DirectPlace 加 photos → 提取 |
| 修改 | `src/adapters/amap/direct.ts` | show_fields 加 photos |
| 修改 | `src/adapters/amap/card-mapper.ts` | AmapPoi 加 photos → 提取 |

### 测试矩阵 (16 个)

| ID | ★ | 类型 | 主题 |
|----|---|------|------|
| TC-M3b-PR01 | ✓ | Unit | Google PRICE_LEVEL_* → $/$$ |
| TC-M3b-PR02 | ✓ | Unit | AMAP cost → price_level + price_per_person |
| TC-M3b-PR03 | | Unit | Price omitted when unavailable |
| TC-M3b-PR04 | | Unit | fieldMask includes priceLevel |
| TC-M3b-P01 | ✓ | Unit | Google photos in card-mapper |
| TC-M3b-P02 | ✓ | Unit | Google photo URL assembly |
| TC-M3b-P03 | ✓ | Unit | AMAP photos in card-mapper |
| TC-M3b-P04 | | Unit | Photos omitted (not empty array) |
| TC-M3b-P05 | | Unit | fieldMask includes places.photos |
| TC-M3b-P06 | | Unit | AMAP show_fields includes photos |
| TC-M3b-H01 | ✓ | HTTP | 搜索结果含 photos |
| TC-M3b-H02 | ✓ | HTTP | 搜索结果含 price_level |
| TC-M3b-H03 | | HTTP | photos undefined 当无图时 |
| TC-M3b-H04 | | HTTP | 双 provider 含 photos |
| TC-M3b-H05 | | HTTP | 双 provider 含 price |
| TC-M3b-H06 | | HTTP | merge 后字段完整 |

### 验收

- [ ] TC-M3b-* 全部 16 个测试通过
- [ ] price.ts 100%，photos.ts 100%

---

## 批次 3: MVP-4a — 语言路由 + 搜索关键词映射

> 解决中英文 prompt 混杂。依赖 MVP-3a (provider-resolver)。

### 实现

| 操作 | 文件 | 变更 |
|------|------|------|
| 新增 | `src/agent/language-router.ts` | LanguageContext 检测 (CJK 占比 + locale) |
| 新增 | `src/agent/language-router.test.ts` | 6 个测试 |
| 新增 | `src/i18n/search-keywords.ts` | 多语言关键词映射表 (EN/CN/HK/TW) |
| 新增 | `src/i18n/search-keywords.test.ts` | 3 个测试 |
| 修改 | `src/core/tools.ts` | 搜索入口集成 language-router |
| 修改 | `src/core/itinerary-timed.ts` | 移除硬编码中文；从 search-keywords 取值 |
| 修改 | `src/agent/loop.ts` | TOOL_DEFS → getToolDefs(locale) |

### 测试矩阵 (12 个)

CJK 检测、locale 映射、关键词翻译 (4 语言)、QLP-G 双语输出、QLP-A 纯中文、HTTP 集成 (3)

---

## 批次 4: MVP-4b — 行程泛化

> 去掉 5 个硬编码城市限制 + 餐食上下文匹配。依赖 MVP-4a。

### 实现

| 操作 | 文件 | 变更 |
|------|------|------|
| 新增 | `src/core/meal-context.ts` | 餐食关键词 + 时段规则 |
| 新增 | `src/core/meal-context.test.ts` | 6 个测试 |
| 修改 | `src/core/itinerary-timed.ts` | 移除城市硬编码；通用模板 + meal-context |
| 修改 | `src/core/itinerary.ts` | 景点间 ≥30min 校验 |

### 测试矩阵 (10 个)

非硬编码城市行程、早餐不含 fine dining、晚餐时段 18:00–20:00、景点间距

---

## 批次 5: MVP-5a — Admin API 加固

> 独立批次，可与 MVP-4 并行。

### 实现

| 操作 | 文件 | 变更 |
|------|------|------|
| 新增 | `src/lib/api-error-handler.ts` | Prisma NotFound→404, Unique→409 |
| 新增 | `app/admin/error.tsx` | Admin Error Boundary |
| 修改 | `app/api/admin/**/*.ts` (6 路由) | 包裹 error handler |
| 修改 | `app/api/admin/password/reset/route.ts` | token 过期 1h → 4h |
| 修改 | `src/auth/session-token.ts` | SessionPayload 增加 `iat` |

### 测试矩阵 (8 个)

DELETE 不存在→404、PATCH unique→409、Error Boundary 渲染、reset token 4h、session iat

---

## 批次 6: MVP-5b — 测试补全

> 纯测试，不改生产代码。独立批次。

### 实现

| 新增 | 文件 | 覆盖 |
|------|------|------|
| 新增 | `src/adapters/tripadvisor/match.edge-cases.test.ts` | 73% → ≥90% |
| 新增 | `tests/integration/vendor-fallback.test.ts` | Google 失败 → AMAP 回退 |
| 新增 | `tests/integration/multi-turn-chat.test.ts` | 上下文保持 |

### 测试矩阵 (12 个)

Tripadvisor 匹配边界、vendor 回退链、多轮对话状态

---

## 批次 7: MVP-6a — TripAdvisor → MCP tool

> 依赖 MVP-3b (photos fallback chain)。

### 实现

| 操作 | 文件 | 变更 |
|------|------|------|
| 新增 | `src/mcp/tools/enrich-place.ts` | 核心逻辑抽取 |
| 新增 | `src/mcp/tools/enrich-place.test.ts` | 3 个测试 |
| 修改 | `src/mcp/create-server.ts` | 注册 `enrich_place` tool |

### 测试矩阵 (5 个)

MCP enrich_place 调用、rating 返回、photos fallback、无匹配处理、HTTP 对等

---

## 批次 8: MVP-6b — E2E 补全

> 纯测试，不改生产代码。独立批次。

### 实现

| 新增 | 文件 | 覆盖 |
|------|------|------|
| 新增 | `e2e/password-reset.spec.ts` | 重置→邮件→设密码→登录 |
| 新增 | `e2e/admin-invite.spec.ts` | 邀请→注册→登录 |
| 新增 | `e2e/caller-integration.spec.ts` | what2eat 模拟调用 |

### 测试矩阵 (8 个)

密码重置全流程、邀请全流程、caller 搜索+photos+price 验证

---

## 批次 9: MVP-7 — Prompt 组装器 + 收尾

> 依赖 MVP-4a (language-router)。最后一批。

### 实现

| 操作 | 文件 | 变更 |
|------|------|------|
| 新增 | `src/agent/prompt-assembler.ts` | 上下文感知 prompt 组装 |
| 新增 | `src/agent/prompt-assembler.test.ts` | 5 个测试 |
| 新增 | `prompts/chat/v1.zh.md` | 中文 system prompt |
| 修改 | `src/agent/loop.ts` | 集成 prompt-assembler |
| 修改 | `prompts/chat/v1.md` → `v1.en.md` | 重命名 |

### 测试矩阵 (8 个)

Prompt 按语言+意图+时段组装、中文 prompt 加载、全量回归

### 最终验收

- [ ] `make quality` 全绿
- [ ] `npm run build` 零 error/warning
- [ ] 全量 E2E 通过
- [ ] README 更新架构图 + MCP 工具列表

---

## 风险

| 风险 | 缓解 |
|------|------|
| Google Photos API 额外计费 | feature flag `GOOGLE_PHOTOS_ENABLED`；先实现 AMAP 图片 |
| 语言检测误判（新加坡中英混合） | 显式 locale override；混合内容双 provider 查询 |
| Prompt 组装增加 token 消耗 | 监控 token usage；模板精简 |

## 验证方式

每批完成后：
1. `make quality` — typecheck + lint + test-coverage + test-e2e
2. 该批次 TC-M*-* 测试矩阵全绿
3. `npm run build` 零 error
